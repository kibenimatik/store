<h3>Home</h3>
<table class="table">
  <% @products.each do |product| %>
    <tr>
      <td><%= product.vendor %></td>
      <td><%= product.model %></td>
      <td><%= number_to_currency(product.price_in_cents/100.to_f) %></td>
    </tr>
  <% end %>
</table>
